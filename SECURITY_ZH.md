# 安全策略

> 英文版(仓库主版本):[SECURITY.md](SECURITY.md)

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 0.4.x(CE 最新发布) | ✅ |
| < 0.4.0 | ❌ 请升级到最新版 |

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。**

请通过 GitHub 私有漏洞报告(Goal 仓库 Security 标签页 → Report a vulnerability)提交,或联系维护者邮箱。请包含:

- 问题类型(如 SQL 注入、XSS、越权访问、敏感信息泄露)
- 复现步骤 / PoC
- 影响版本与部署形态(Docker Compose / 源码部署)
- 如有可能,修复建议

我们会在 **3 个工作日内**确认收到,**7 天内**给出初步评估,修复进度会同步给报告人。

## 部署加固建议

- 生产环境务必修改 `YH_JWT_SECRET`(`openssl rand -hex 32` 生成),不要用模板默认值。
- `/api/v1/p/*` 平台面(EE/SaaS)配置 IP 白名单后再暴露。
- 不要把 `.env`、`config/*.yaml` 中的真实密钥提交进仓库或镜像。
